import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'menu_navigation_coulissant',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
